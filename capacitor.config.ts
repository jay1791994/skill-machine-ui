import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skillmachine.app',
  appName: 'skill-machine-ui',
  webDir: 'dist/skill-machine-ui/browser'
};

export default config;
